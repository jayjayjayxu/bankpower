# Runtime dependencies. Document parsing and vector-index construction remain
# offline maintenance tasks. The legacy BankAI retrieval path is still served
# by this API, so its *query-time* embedding dependencies must be present.
fastapi==0.115.12
uvicorn[standard]==0.34.0
httpx==0.28.1
openai==2.21.0
PyYAML==6.0.3
sqlglot==30.17.0
PyMySQL==1.1.2
cryptography==46.0.5

# BankAI V0.4 semantic retrieval. The BGE model and FAISS index are mounted
# read-only at runtime; these packages only provide CPU inference/search.
# The production image is Python 3.11; 2.5+ requires Python 3.12.
numpy==2.4.6
faiss-cpu==1.15.0
torch==2.11.0
transformers==5.12.0
huggingface_hub==1.19.0
