# 当前电力网站地址；部署到服务器时使用 http://121.40.255.180
VITE_POWER_SITE_URL=http://172.20.10.2:5173
# 部署在同域子路径时使用 /compute/；本地开发可保留 /
VITE_APP_BASE=/
# 部署在同域子路径时使用 /compute/api；本地开发可保留 /api
VITE_API_BASE_URL=/api
